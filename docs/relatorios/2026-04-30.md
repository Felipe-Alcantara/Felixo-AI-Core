# Relatório: 2026-04-30

## Resumo

Terceiro dia de atividade do repositório. O trabalho consolidou continuidade de CLIs, processos persistentes, arquitetura híbrida com MCP, adapters persistentes fake/ACP/app-server, opções runtime de modelo e a tasklist de UX/workspace de 30/04/2026.

Fonte: commits de 2026-04-30 no Git.

## Implementações

### CLI, threads e persistência

- Corrigida contagem/contexto determinístico no Codex.
- Classificação e supressão de stderr não acionável do Codex.
- Preservado diff de projetos ao trocar modelo.
- Desabilitada retomada instável do Gemini até haver protocolo confiável.
- Adicionado terminal por threads e persistência real para Claude.
- Habilitada retomada nativa para Codex e Gemini quando há metadados de sessão.

### Arquitetura híbrida e MCP

- Separados Terminal Adapters e planejamento de execução.
- Adicionado catálogo MCP inicial do Felixo.
- Documentada arquitetura híbrida MCP + CLIs.
- Registrados limites de processos persistentes e retomada nativa.

### Adapters persistentes e protocolos fake

- Adicionados fake persistent agents e tipos `AgentEvent`.
- Implementado adapter Gemini ACP.
- Implementado adapter Codex app-server persistente e fake agent.
- Ajustado parsing do Codex app-server com base no protocolo real.
- Renderer passou a reconhecer tipos persistentes de CLI.

### Opções runtime de modelo

- Adicionado suporte backend a opções runtime de modelo.
- Composer ganhou seletores de modelo do provedor e effort.
- Criado plano de confirmações/permissões para CLI.

### Limpeza operacional

- `start_app.py` passou a limpar processos antigos antes do launch.
- README foi atualizado.

### Chat e contexto

- "Novo chat" limpa input, anexos, terminal local e estado de streaming.
- A thread corrente é enviada ao backend por `cli:reset-thread` para encerrar sessão persistente anterior quando houver.
- Anexos do botão `+` no composer entram como contexto da próxima mensagem.
- Arquivos de texto pequenos recebem preview textual truncado; binários entram como metadados.

### Projetos

- Projetos cadastrados persistem via `project-storage.ts`.
- IDs de projetos ativos também persistem e são validados contra a lista atual.
- Sidebar exibe projetos ativos mesmo com a seção colapsada.

### Automações

- Criado catálogo de automações padrão em `data/automations.ts`.
- Criado storage de automações personalizadas em `automation-storage.ts`.
- `AutomationsModal` lista automações padrão/customizadas, cria customizadas e aplica prompts no composer.

### Modelos e Felixo

- Clicar em um modelo na sidebar seleciona o modelo e abre `ModelSettingsModal`.
- O modal de modelos mostra capacidades por CLI e campos configuráveis (`providerModel`, `reasoningEffort`).
- O botão "Felixo" abre `FelixoSettingsModal`, separado das configurações de modelos.

### Code/Git

- Criado `CodePanel` para consulta inicial de Git.
- Criado IPC `git:get-summary`.
- Criado `git-service.cjs` com comandos allowlisted e read-only:
  - `status --short --branch`;
  - `diff --stat`;
  - `log -5 --oneline --decorate=short`;
  - `branch --show-current`.
- Adicionados testes unitários para parsing e allowlist Git.

### Terminal e QA Logger

- Terminal lateral alterna entre `Threads` e `Orquestrador`.
- Terminal pode ser recolhido e redimensionado horizontalmente.
- QA Logger pode ser recolhido e redimensionado verticalmente.
- QA Logger preserva assinatura de eventos mesmo recolhido.

### Interface

- Janela principal Electron configurada explicitamente como redimensionável, maximizável e fullscreen.
- Selects nativos receberam tema escuro para corrigir fundo branco/texto branco.
- Placeholder visual da tela inicial foi substituído pela logo do portfólio em `/brand/felixo-logo.png`.

## Commits do dia

| Commit | Descrição |
|--------|-----------|
| `8e8bdc0` | fix(cli): make Codex context and counts deterministic |
| `4dbf618` | fix(codex): classify non-fatal stderr output |
| `8f9e3b6` | fix(codex): suppress non-actionable stderr noise |
| `710b3e7` | fix(chat): preserve project diff across model changes |
| `663b181` | fix(gemini): keep unstable resume disabled |
| `f2292f8` | feat(cli): add threaded terminal output and Claude persistence |
| `d562c6c` | docs(project): document CLI persistence status |
| `b5d1f90` | feat(cli): enable native resume for Codex and Gemini |
| `a7f33cf` | docs(cli): describe native resume and persistent process limits |
| `6258e73` | docs(cli): map persistent CLI protocols |
| `66c0f21` | refactor(cli): split terminal adapters and execution planning |
| `1bd0c7b` | feat(mcp): add Felixo tool catalog |
| `22e2af5` | docs(architecture): document hybrid MCP orchestration |
| `84b2cde` | docs: register recent commits and refine architecture details |
| `eeb44a3` | feat(adapters): add fake persistent agents, AgentEvent types, and Gemini ACP adapter |
| `e99793c` | feat(adapters): add Codex app-server persistent adapter and fake agent |
| `05daf4e` | fix(adapters): align Codex app-server parsing with real protocol |
| `fbdc133` | feat(renderer): recognize persistent CLI adapter types |
| `97d9ffe` | feat(adapters): support runtime model options |
| `fd9340b` | feat(chat): add runtime model selectors |
| `b777f30` | docs(backend): plan cli permission confirmations |
| `8cf8dbc` | chore(app): clean stale processes before launch |
| `9ca7657` | Update README.md |
| `6d79cfe` | feat(electron): prepare thread reset and git summary |
| `dd331fd` | feat(chat): add workspace automation and code panels |
| `c305318` | docs: record tasklist 2026-04-30 |

## Arquivos principais alterados

- `app/src/features/chat/components/ChatWorkspace.tsx`
- `app/src/features/chat/components/AppSidebar.tsx`
- `app/src/features/chat/components/Composer.tsx`
- `app/src/features/chat/components/ModelSettingsModal.tsx`
- `app/src/features/chat/components/TerminalPanel.tsx`
- `app/src/features/chat/components/QaLoggerPanel.tsx`
- `app/src/features/chat/components/AutomationsModal.tsx`
- `app/src/features/chat/components/CodePanel.tsx`
- `app/src/features/chat/components/FelixoSettingsModal.tsx`
- `app/src/features/chat/services/project-storage.ts`
- `app/src/features/chat/services/automation-storage.ts`
- `app/electron/services/git-service.cjs`
- `app/electron/services/git-ipc-handlers.cjs`
- `app/electron/services/ipc-handlers.cjs`
- `app/electron/preload.cjs`
- `app/electron/core/window-options.cjs`

## Validação

- `npm test`: passou com 118 testes.
- `npm run build`: passou.
- `npm run lint`: passou.
- `git diff --check`: passou.

## Pendências recomendadas

- Persistir histórico de sessões em storage versionado do Electron.
- Evoluir o painel Code para operações Git com escrita somente após política de confirmação.
- Adicionar testes de UI para fluxo de anexos, automações e painéis recolhíveis.
