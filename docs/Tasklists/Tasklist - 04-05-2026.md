Legenda:

- `[x]` concluído ou entregue no escopo definido.
- `[ ]` pendente ou parcialmente entregue.
- Itens marcados como plano podem estar concluídos como documentação, mas ainda pendentes como implementação futura.

Lista de pendencias atual para investigar e implementar

sempre que for implementar algo, certifique-se de sempre estar no padrão de qualidade da pasta pasta /docs

documentar tudo antes e depois de implementar

fazer commit por feature/implementação

Analize, investigue, plenaje e implemente tudo que está no guia seguindo o padrão de qualidade da pasta /docs

Tasklist geral

Tasklist

[x] - Clicar em novo chat deve limpar todo o possivel cache do chat enterior, utilizar apenas as memorias globais para auxiliar o modelo na resposta

[x] - A barra lateral da esquerda não tem barra de rolagem, os itens estão saindo da tela

[x] - preciso de um botão novo para skills, para eu conseguir incluir um superprompt que vai ensinar o modelo a usar alguma ferramenta ou criar algo do 0 - Skill significa em prompt que ensine o agente a usar alguma ferramenta, plataforma, estilo, etc

[x] - Esse botão "Orquestrador" enta com muitas opções que dificilmente vão ser usadas por estar confuso demais, a parte de "Modelos para spawn" até pode ser útil, mas "Contexto" e "Limites" ainda está muito abstrato podemos acoplar essa função a função de memoria global para deixar tudo em um lugar só que fará mais sentido

[x] - A parte de chats recentes pode aparecer apenas os ultimos 5, e um botão para aparecer todos, pode ureutilizar o modal da pesquisa para o usuario encontrar algum chat passado

[x] - "Modelos
Configurar modelos

Claude Code CLI (Anthropic - oficial)
CLI local

Codex CLI (OpenAI - oficial)
CLI local

Gemini CLI (Google - oficial)
CLI local

Codex CLI
CLI oficial instalada no sistema

Codex App Server
CLI oficial instalada no sistema

Claude Code CLI" essa parte de modelos, evitar duplicatas - e adicionar uma função de apagar um modelo sem precisar abrir as configurações dos modelos 

[x] - Revisar a área de "Modelos
Configurar modelos

Claude Code CLI (Anthropic - oficial)
CLI local

Codex CLI (OpenAI - oficial)
CLI local

Gemini CLI (Google - oficial)
CLI local

Codex CLI
CLI oficial instalada no sistema

Codex App Server
CLI oficial instalada no sistema

Claude Code CLI" pois o botão de instalar estava bugado no outro pc por poder instalar um modelo que já estava instalado

[x] - Obrigar o orquestrador a se comunicar usando markdown organizado, o texto está muito corrido

[x] - Implementar o vizualizador de markdown no chat para as respostas, separar topicos, vizualizador de bash, código, titulos e topicos

[x] - Verificar como o orquestrador lida com arquivos inseridos, indicar melhor onde e como os arquivos devem ser interpretados pelo orquestrador

[x] - Fazer o orquestrador não mostrar toda a linha de raciocínio no chat e sim na aba de "Terminal
0" - ele só enviará o conteúdo das mudanças e como estava antes e como ficou depois

[x] - Melhorar a comunicação entre o agente e as threads que ele abrir, podendo fazer decisões tecnicas antes de depois da resposta do sub-agente que ele spawnar e verificar se essa modificação/tarefa faz sentido para o usuario na hora da resposta

[x] - Também não quero que ele esconda nada, o que for de background mostrar no "Terminal
0" e o que for resposta útil para o usuario mostrar no chat, mas sem esconder nenhum processo

[x] - A função de code pode implementar funções basicas de git
